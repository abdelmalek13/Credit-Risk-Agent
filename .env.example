# Google Gemini API key (required if using Gemini provider)
GOOGLE_API_KEY=your_google_api_key_here

# LLM provider: "gemini" or "local"
# "local" uses Qwen2.5-Coder 1.5B (auto-downloads ~1 GB on first use)
LLM_PROVIDER=gemini
